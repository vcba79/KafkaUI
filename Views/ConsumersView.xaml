<UserControl x:Class="KafkaUI.Views.ConsumersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:KafkaUI">
    <UserControl.Resources>
        <ResourceDictionary Source="/Themes/DarkTheme.xaml"/>
    </UserControl.Resources>

    <Grid Background="{StaticResource Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Consumers"
                   FontSize="22" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimary}"
                   Margin="24,24,24,16"/>

        <!-- Search -->
        <TextBox Grid.Row="1"
                 Style="{StaticResource DarkTextBox}"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 Tag="Search"
                 Margin="24,0,24,16"
                 Width="280" HorizontalAlignment="Left"/>

        <!-- Table -->
        <Border Grid.Row="2" Margin="24,0,24,24"
                Style="{StaticResource Card}" Padding="0">
            <DataGrid Style="{StaticResource DarkDataGrid}"
                      ItemsSource="{Binding FilteredGroups}"
                      SelectedItem="{Binding SelectedGroup}"
                      CanUserResizeRows="False">
                <DataGrid.Columns>

                    <!-- Consumer Group ID -->
                    <DataGridTemplateColumn Header="Consumer Group ID" Width="*" MinWidth="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding GroupId}"
                                           Foreground="{StaticResource Accent}"
                                           FontSize="13" Cursor="Hand"
                                           TextTrimming="CharacterEllipsis"
                                           VerticalAlignment="Center"
                                           Margin="12,0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Num Of Members -->
                    <DataGridTextColumn Header="Num Of Members"
                                        Binding="{Binding MemberCount}"
                                        Width="140">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="12,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Num Of Topics -->
                    <DataGridTextColumn Header="Num Of Topics"
                                        Binding="{Binding NumOfTopics}"
                                        Width="130">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="12,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Messages Behind -->
                    <DataGridTextColumn Header="Messages Behind"
                                        Binding="{Binding TotalLag}"
                                        Width="140">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="12,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Coordinator -->
                    <DataGridTextColumn Header="Coordinator"
                                        Binding="{Binding Coordinator}"
                                        Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="12,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- State badge -->
                    <DataGridTemplateColumn Header="State" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border VerticalAlignment="Center" HorizontalAlignment="Left"
                                        CornerRadius="3" Padding="8,3" Margin="12,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#2D2200"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding State}" Value="STABLE">
                                                    <Setter Property="Background" Value="#2D2200"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding State}" Value="EMPTY">
                                                    <Setter Property="Background" Value="#22263A"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding State}" Value="DEAD">
                                                    <Setter Property="Background" Value="#2D1010"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding State}" Value="REBALANCING">
                                                    <Setter Property="Background" Value="#1A2D3A"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding State}" FontSize="11" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{StaticResource Warning}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding State}" Value="STABLE">
                                                        <Setter Property="Foreground" Value="{StaticResource Warning}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding State}" Value="EMPTY">
                                                        <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding State}" Value="DEAD">
                                                        <Setter Property="Foreground" Value="{StaticResource Danger}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding State}" Value="REBALANCING">
                                                        <Setter Property="Foreground" Value="{StaticResource Accent}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
